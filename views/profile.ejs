<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full h-screen bg-zinc-800 px-20 py-16">
        <div class="flex justify-end w-full"><a class="px-4 py-2 text-white bg-red-700 mb-4 rounded-md inline-block" href="/logout">Log Out</a>
        </div>
        <h1 class="text-4xl text-white tracking-tighter mb-5"><span class="text-red-500 text-6xl">Hello,</span> <%=user.name%></h1>
        <h1 class="text-xl text-white tracking-tighter mb-5">Create a New Post </h1>
        <form action="/post" method="post">
            <textarea name="content" placeholder="Whats in your mind" class="p-3 resize-none bg-transparent border-2 border-zinc-600 rounded-md w-1/3   text-white" name="" id=""></textarea>
            <input value="Create Your Post" type="submit" class="px-6 py-3 bg-blue-700 block text-white rounded-md  ">
        </form>
        <div class="posts mt-20">
            <h1 class="text-zinc-400 text-xl">Your Posts</h1>
            <div class="postcontainer">
                <%user.posts.reverse().forEach(function(post){%>
                    <div class="post w-1/3 p-4 mb-2 rounded-md border-2 border-zinc-600 bg-zinc-700">
                        <h4 class="text-blue-400 tracking-tighter">@<%=user.username%></h4>
                        <p class="text-white"><%=post.content%></p>
                        <div class="btns mt-6 flex justify-between">
                            <a class="text-blue-500" href="">Like</a>
                            <a class="text-zinc-400" href="">Edit</a>
                        </div>
                    </div>
                <%})%>
                
            </div>
        </div>
    </div>
</body>
</html>

